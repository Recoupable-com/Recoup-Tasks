/**
 * Shared logger re-export.
 * All task code should import { logger } from "@/lib/logger"
 * so the underlying provider can be swapped in one place.
 */
export { logger } from "@trigger.dev/sdk/v3";
